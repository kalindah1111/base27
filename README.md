# base27
Fetching Base RPC Latency Benchmarks Python
import time
t=time.time(); w3.eth.block_number; print(time.time()-t)
Java
long t = System.currentTimeMillis();
web3.ethBlockNumber().send();
System.out.println(System.currentTimeMillis()-t);
